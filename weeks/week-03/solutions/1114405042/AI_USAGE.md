# AI 使用紀錄 (AI Usage Log) - Week 03 作業

## 1. 寫下你使用的 AI 助手工具
* **使用的 AI 工具**：GitHub Copilot (Gemini-3.1-pro)

## 2. 你用 AI 幫忙解決了什麼問題？
請詳述你如何使用 AI，並列出至少三個具體情境。

1. **題目理解與解題策略設計 (含 -easy 版)**：
   * **UVA 118**：請 AI 幫助我實作座標與方向旋轉邏輯。AI 提供了標準版的陣列向量 (`dx`, `dy`) 計算方式，並額外幫我撰寫了 `-easy` 版本（利用 `"NESW"` 字串和對應索引），大幅簡化了左右轉的實作，方便背誦。
   * **UVA 299**：起初對「最少交換次數」沒有頭緒，AI 點出這其實就是「逆序對 (Inversions)」的問題，並幫我設計了簡潔的雙層迴圈 `-easy` 版本。
   * **UVA 490**：AI 建議使用 `splitlines()` 來保留換行以外的所有空白字元，並利用找出最大長度的方式動態填補空格，解決了長短句旋轉可能遇到的 Index 錯誤。

2. **撰寫單元測試 (Unit Tests) 與極端測資設計**：
   * 針對每一個 UVA 題目 (118, 272, 299, 490)，我請 AI 協助撰寫 `unittest` 框架 (`test_118.py` 等)。
   * AI 幫我考量了多種邊界狀況：UVA 272 的「無引號」及「連續嵌套引號」、UVA 299 的「車廂數為 0」及「已排好序」、UVA 490 的「中間句子較短」需補空白的測試等。

3. **優化程式碼與撰寫詳細註解**：
   * 在實作過程中，AI 協助檢查程式碼中的型別與邊界，並主動加上了**非常詳盡的繁體中文註解**，讓我能輕易了解每一行程式碼的功用。例如 UVA 272 中提醒使用三元運算子 ` "``" if is_left else "''" ` 能夠讓 `-easy` 版的代碼無比精簡。

## 3. 審思 AI 的建議
* **AI 給的解答有沒有錯誤或需要調整的地方？**
  * 在一開始撰寫 `importlib` 來載入有 `-` 符號的模組時，AI 遇到了一些 LSP 型別警告 (例如 `ModuleSpec` 有可能為 `None`)。我請 AI 修正並加上了 `if spec_xxx and spec_xxx.loader:` 的防呆機制，最後成功排除警告並完成測試。
* **你從 AI 的建議中學到了什麼？**
  * 學習到遇到 `import` 含有特殊字元（如 `-easy`）的檔案時，可以透過 `importlib.util` 進行手動模組匯入。
  * 體會到「邏輯精簡化」的重要性，`-easy` 版本的設計理念非常適合應付上機考。
  * 理解了如何使用 Python 的 `reversed()` 和動態字串拼接 (`row += " "`) 來處理複雜的陣列旋轉問題。
